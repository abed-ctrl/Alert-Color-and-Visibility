# Exercice 3 - Alert Color && Visibility
React App that give the user a list of Bootstrap colors to choose and apply it to a Bootstrap Alert. in Addition we add visibility option to show/hide the alert.
